# ──────────────────────────────────────────────────
# Kubernetes Deployment for Courses Platform API
# Runs on AKS with secrets from Kubernetes Secrets
# ──────────────────────────────────────────────────
apiVersion: apps/v1
kind: Deployment
metadata:
  name: courses-api
  labels:
    app: courses-api
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: courses-api
  template:
    metadata:
      labels:
        app: courses-api
    spec:
      containers:
      - name: courses-api
        image: coursesacrehab.azurecr.io/courses-api:latest
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        env:
        - name: ASPNETCORE_ENVIRONMENT
          value: "Production"
        - name: CosmosDb__ConnectionString
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: cosmos-connection
        - name: BlobStorage__ConnectionString
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: blob-connection
        - name: Cdn__Endpoint
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: cdn-endpoint
        - name: Cdn__BlobHost
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: blob-host
        - name: CognitiveServices__Endpoint
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: cognitive-endpoint
        - name: CognitiveServices__ApiKey
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: cognitive-key
        - name: AzureAd__TenantId
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: aad-tenant-id
        - name: AzureAd__ClientId
          value: "a3315a3b-660f-4fcd-9705-04a3df3f3c89"
        - name: AzureAd__Audience
          value: "api://a3315a3b-660f-4fcd-9705-04a3df3f3c89"
        - name: AzureAd__Instance
          value: "https://coursesappehab.ciamlogin.com/"
        - name: AzureAd__Domain
          value: "coursesappehab.onmicrosoft.com"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
